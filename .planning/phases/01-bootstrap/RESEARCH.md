# Research

GSD is the execution/state base because its current Codex path uses standalone per-agent TOMLs, a bare `[agents]` dispatch table, fresh worker contexts, explicit execution artifacts, and strong recovery/isolation concepts.

OMC contributes topology/assumption convergence, architect/critic role separation, specialized code/security review, bounded verify/fix thinking, onboarding, and persistent wiki discipline.

The current remote environment has no `codex` executable in PATH, so concrete model IDs are intentionally not pinned.
