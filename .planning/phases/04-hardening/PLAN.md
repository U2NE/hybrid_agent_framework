# PLAN — Hardening

1. Replace role-heavy routing with stage-local Luna effort ladder and verified fallback.
2. Make classification evidence-based and installer namespace-safe.
3. Minimize Tier 0/1 pipelines and add markdown-aware context reduction plus conditional isolation.
4. Add bounded planning consensus, reviewer protocols, acceptance traceability, security trigger precision, and edge-probe gates.
5. Add ontology convergence and semantic runtime validators.
6. Run authenticated routing probe and live A/B/C.
7. Re-run full deterministic suite, strict config, wiki lint, state recovery, then update durable docs/state.
