# PLAN — Hardening

1. Replace role-heavy routing with a stage-local Luna effort ladder and fail-closed explicit model/effort routing.
2. Make classification evidence-based and installer namespace-safe.
3. Minimize Tier 0/1 pipelines and add markdown-aware context reduction plus deterministic scheduling/isolation.
4. Add bounded planning consensus, reviewer protocols, acceptance traceability, security trigger precision, and edge-probe gates.
5. Add ontology convergence and semantic runtime validators.
6. Seal explicit user-approved execution into a deterministic graph, bind role capability grants, and evolve the execution schema to `hybrid-exec-graph/v4` with sealed scheduler `isolationMode`.
7. Add durable task leases, graph-revision fencing, material-revision approval, Sol-budget authority, and lease-extension barriers so execution cannot drift from the sealed graph.
8. Add repository-global current-workspace mutation guarding plus durable worktree ownership/integration records; bind integration ordering and recovery evidence to task `attemptId` and non-secret `leaseId`.
9. Add durable terminal transition fencing and evidence-bound lease release; require exact active dispatch authority for new terminal outcomes and completed integration evidence for all successful worktree terminals.
10. Add Browser Functional QA and Browser Adversarial QA as separate read-only review lanes, with a bounded Playwright-compatible provider, safe same-origin automatic interaction, explicit destructive-action authorization, CLI/install propagation, and verifier-gated browser evidence.
11. Run authenticated runtime validation through A/B/C and E~K, preserving scope boundaries: G/H validate the framework-source generic quality primitive, I validates installed-Lead production-primitive use, and J/K validate installed decision provenance/audit.
12. Keep real user-interactive Case D pending rather than fabricating user answers, and do not claim backend serving-model identity beyond accepted explicit model/effort requests.
13. Re-run the full deterministic regression, unit suite, syntax checks, state recovery, documentation drift checks, and durable docs/state synchronization.
