---
title: Architecture
category: architecture
tags: [codex, orchestration, routing, architecture]
updated: 2026-09-25
---
# Architecture

Hybrid uses lead-owned flat sibling dispatch, tier-aware minimal pipelines, dependency-aware execution waves, conditional worktree isolation, markdown-aware fresh-context reduction, Luna effort-ladder routing with conditional Sol escalation, independent verification, conditional security review, and canonical `.planning/` state.

Independent tasks are parallel-eligible in the same wave; dependencies move work to later waves; same-file writers serialize; cycles reject.

Authenticated Codex runtime history covers A/B/C spawn/handoff semantics, sibling parallelism, same-file serialization, quality lanes, Luna effort override acceptance, revised Case J Implementer-owned Tier 0 provenance, and Case K two-child parallel provenance. The former rejected-model → no-override fallback is superseded by current explicit allowlisted model/effort fail-closed enforcement. J/K outer Leads and Implementer dispatches explicitly request Luna/medium; their current persisted audits are clean. Real user-interactive Case D remains intentionally pending, and backend serving-model identity is not independently attested.

See [[index]] and [[decisions]].


Clarification now uses the pinned OMC deep-interview semantics within Hybrid state: one-time Round 0 topology lock, one weakest component × dimension question per round, full re-scoring after each answer, Scout-first brownfield facts, OMC challenge/stall/stop rules, and explicit approval after a threshold-passing SPEC. Hybrid-specific persistence remains in `.planning/` / `hybrid-state/v1`.
