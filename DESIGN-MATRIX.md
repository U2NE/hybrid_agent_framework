# Design Matrix

Pinned source snapshots used for this audit:

- GSD Core: `9db80da9a047ddaa9cc8812a5d6adab446fe8433`
- OMC: `9fd35ece5d6de65b511bf43b55e42c499e4fc194`

The upstream repositories remain read-only evidence sources. Hybrid-specific adaptations are called out rather than being attributed to upstream behavior.

| Capability | GSD implementation | OMC implementation | Selected Hybrid design | Ported behavior | Deliberately discarded | Verified status / residual risk |
|---|---|---|---|---|---|---|
| Orchestration | Thin orchestrator, fresh workers, phase artifacts | Team/autopilot orchestration | GSD-style lead-owned flat dispatch | OMC role semantics; independent QA lanes | recursive/team-first default | Authenticated A/B/C live smoke verified lead-owned workers and no recursive delegation. |
| Requirements | Spec phase, weighted ambiguity gate, scout-first | Deep interview, topology, assumptions, ontology | Hybrid ambiguity gate + OMC clarification semantics | Round 0 topology, weakest-dimension targeting, challenge modes, ontology convergence | mandatory interview for every task | Deterministic Case D passes; real user-interactive D remains intentionally unsimulated. |
| Classification | Bounded phase selection | Mode-oriented routing | Evidence/anchor-based tiers | concrete files/symbols/behavior/constraints as evidence | raw file-count and broad keyword escalation | Regression fixtures cover vague vs bounded, localized refactor, and mechanical multi-file changes. |
| Planning | Planner + checker with file/dependency metadata | Planner + architect + critic consensus | One canonical PLAN with conditional bounded council | reject -> revise -> independent re-review; explicit required reviewer set; 3/5 iteration caps; principles/drivers/options/pre-mortem/ADR | duplicate plan formats; role-name-driven council on small tasks | Capped rejection cannot become approval; incomplete required council reviews and acceptance coverage fail closed. |
| Scheduling | Dependency waves, worktree isolation, file scope | Team worker orchestration | Dependency/file-ownership scheduler | role-aware handoffs; risk-based worktree isolation | agent-count-driven concurrency | Authenticated A proves sibling overlap; B proves same-file serialization. Authenticated Case E verifies real detached worktree creation, separate worker cwd paths, concurrent authenticated workers, declared-owner patch handoff, fail-closed ownership/integration checks, main-workspace verification, cleanup/prune, and no orphan worktrees in a disposable smoke repository. Arbitrary production-repository merge safety is not claimed. |
| Context | Fresh phase/worker context | Role prompts/session context | Budgeted fresh worker context | preserve Goal, Acceptance, Constraints, files/interfaces, dependencies, decisions, verification | raw transcript/prefix dumps | Markdown-aware reduction regression-tested; critical sections are never sacrificed for low-priority history. |
| State | Durable `.planning/` recovery | OMC session/state artifacts | GSD-style canonical `hybrid-state/v1` | compact clarification state inside existing schema | second canonical state store | Recovery/schema corruption handling verified fail-closed. |
| Verification | Executor/verifier separation | Tester, code reviewer, verifier, fix loop | Independent evidence pipeline | fresh verification, VERIFIED/PARTIAL/MISSING trace, bounded fix loop | implementer self-approval | A/B/C live reports include independent verifier completion; deterministic trace gates are fail-closed. |
| Security | Security capability/gates | Specialized security reviewer | Conditional security lane | strong/contextual trigger split; focused checklist; severity x exploitability x blast radius | review on every keyword hit | Authenticated C completed tester + code reviewer + Luna-max security reviewer + verifier. Weak documentation/label triggers are regression-tested false. |
| Knowledge | Planning docs | Persistent wiki ingest/query/lint | Wiki as projection over canonical state/docs | wiki lint/query/ingest and durable decision summaries | wiki as source of truth | Wiki lint verified; stale-projection risk remains operational. |
| Codex agents | Standalone TOML + config registration | Claude-oriented role prompts | Current Codex 0.156.1 standalone agents | adapted role contracts and lead-only sibling dispatch | Claude aliases / nested Task assumptions | Strict config, install namespace preservation, authenticated spawn/handoff all verified. |
| Model routing | Session inheritance + runtime override surface | Role/model preferences | Stage-local Luna effort ladder, Sol only when warranted | Luna medium -> high -> xhigh -> max, then Sol high/xhigh/max; failure-driven escalation; no-override fallback | sticky heavy roles; scattered model IDs | Authenticated Luna high/xhigh/max override requests accepted. Deliberate invalid model rejection followed by no-override session-inheritance retry succeeded. Backend serving identity is not independently attested. |
